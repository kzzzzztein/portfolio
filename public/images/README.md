All project screenshots are in place. To update one later, just overwrite
the matching file here with the same filename (see src/data/projects.js
for which file maps to which project/label), then rebuild and redeploy.
